version: '3.1'

services:
#region Micro-service Products
  micro-products:
    image: micro-products
    container_name: micro-products
    restart: always
    ports:
      - 3001:3001
    depends_on:
      - micro-products-db
    environment:
      - MONGO_URL=mongodb://root:root@micro-products-db:27017/products?authSource=admin

  micro-products-db:
    image: mongo:4.4.10
    container_name: micro-products-db
    restart: always
    ports:
        - 27001:27017
    environment: 
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: products

  micro-products-ui-db:
    image: mongo-express:latest
    container_name: micro-products-ui-db
    restart: always
    ports:
        - 8081:8081
    environment:
        ME_CONFIG_MONGODB_SERVER: micro-products-db
        ME_CONFIG_MONGODB_PORT: 27017
        ME_CONFIG_MONGODB_ADMINUSERNAME: root
        ME_CONFIG_MONGODB_ADMINPASSWORD: root
    depends_on: 
        - micro-products-db
#endregion Micro-service Comments

#region Micro-service Comments
  micro-comments:
    image: micro-comments
    container_name: micro-comments
    restart: always
    ports:
      - 3002:3002
    depends_on:
      - micro-comments-db
    environment:
      - MONGO_URL=mongodb://root:root@micro-comments-db:27017/comments?authSource=admin

  micro-comments-db:
    image: mongo:4.4.10
    container_name: micro-comments-db
    restart: always
    ports:
        - 27002:27017
    environment:
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: comments

  micro-comments-ui-db:
    image: mongo-express:latest
    container_name: micro-comments-ui-db
    restart: always
    ports:
        - 8082:8081
    environment:
        ME_CONFIG_MONGODB_SERVER: micro-comments-db
        ME_CONFIG_MONGODB_PORT: 27017
        ME_CONFIG_MONGODB_ADMINUSERNAME: root
        ME_CONFIG_MONGODB_ADMINPASSWORD: root
    depends_on: 
        - micro-products-db
#endregion Micro-service Comments

#region Micro-service Product-Lists
  micro-product-lists:
    image: micro-product-lists
    container_name: micro-product-lists
    restart: always
    ports:
      - 3003:3003
    depends_on:
      - micro-product-lists-db
    environment:
      - MONGO_URL=mongodb://root:root@micro-product-lists-db:27017/product-lists?authSource=admin

  micro-product-lists-db:
    image: mongo:4.4.10
    container_name: micro-product-lists-db
    restart: always
    ports:
        - 27003:27017
    environment: 
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: product-lists

  micro-product-lists-ui-db:
    image: mongo-express:latest
    container_name: micro-product-lists-ui-db
    restart: always
    ports:
        - 8083:8081
    environment:
        ME_CONFIG_MONGODB_SERVER: micro-product-lists-db
        ME_CONFIG_MONGODB_PORT: 27017
        ME_CONFIG_MONGODB_ADMINUSERNAME: root
        ME_CONFIG_MONGODB_ADMINPASSWORD: root
    depends_on: 
        - micro-product-lists-db
#endregion Micro-service Product-List

#region Micro-service Users
  micro-users:
    image: micro-users
    container_name: micro-users
    restart: always
    ports:
      - 3004:3004
    depends_on:
      - mongo-users-db
    environment:
      - MONGO_URL=mongodb://root:root@micro-users-db:27017/users?authSource=admin

  micro-users-db:
    image: mongo:4.4.10
    container_name: micro-users-db
    restart: always
    ports:
        - 27004:27017
    environment: 
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: users

  micro-users-ui-db:
      image: mongo-express:latest
      container_name: micro-users-ui-db
      restart: always
      ports:
          - 8084:8081
      environment:
          ME_CONFIG_MONGODB_SERVER: micro-users-db
          ME_CONFIG_MONGODB_PORT: 27017
          ME_CONFIG_MONGODB_ADMINUSERNAME: root
          ME_CONFIG_MONGODB_ADMINPASSWORD: root
      depends_on: 
          - micro-users-db
#endregion Micro-service User

#region Micro-service Ratings
  micro-ratings:
    image: micro-ratings
    container_name: micro-ratings
    restart: always
    ports:
      - 3005:3005
    depends_on:
      - micro-ratings-db
    environment:
      - MONGO_URL=mongodb://root:root@micro-ratings-db:27017

  micro-ratings-db:
    image: mongo:4.4.10
    container_name: micro-ratings-db
    restart: always
    ports:
        - 27005:27017
    environment: 
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: ratings

  micro-ratings-ui-db:
      image: mongo-express:latest
      container_name: micro-ratings-ui-db
      restart: always
      ports:
          - 8085:8081
      environment:
          ME_CONFIG_MONGODB_SERVER: micro-ratings-db
          ME_CONFIG_MONGODB_PORT: 27017
          ME_CONFIG_MONGODB_ADMINUSERNAME: root
          ME_CONFIG_MONGODB_ADMINPASSWORD: root
      depends_on: 
          - micro-ratings-db
#endregion Micro-service Rating

#region Micro-service Orders
  micro-orders:
    image: micro-orders
    container_name: micro-orders
    restart: always
    ports:
      - 3006:3006
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://micro-orders-db:27017/orders
      - MONGO_USER=root
      - MONGO_PASSWORD=root
      - DB=orders
      - PORT=3006
      - APP_ENV=development

  micro-orders-db:
    image: mongo:4.4.10
    container_name: micro-orders-db
    restart: always
    ports:
        - 27006:27017
    environment: 
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
        MONGO_INIT_DATABASE: orders

  micro-orders-ui-db:
      image: mongo-express:latest
      container_name: micro-orders-ui-db
      restart: always
      ports:
          - 8086:8081
      environment:
          ME_CONFIG_MONGODB_SERVER: micro-orders-db
          ME_CONFIG_MONGODB_PORT: 27017
          ME_CONFIG_MONGODB_ADMINUSERNAME: root
          ME_CONFIG_MONGODB_ADMINPASSWORD: root
      depends_on: 
          - micro-orders-db
#endregion Micro-service Order

